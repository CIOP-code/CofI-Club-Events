# Local development secrets for `wrangler pages dev`
# Copy this file to .dev.vars (git-ignored) and fill in your values.
# Never commit .dev.vars to version control.

JWT_SECRET=this-secret-is-a-secret
ADMIN_PASSWORD=CollegeOfIdaho2024!
